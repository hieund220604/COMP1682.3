import 'package:flutter/material.dart';
import 'package:splitpal/ui/savings/home_page.dart';

/// SavingsTab is the entry point for the Savings feature in the bottom navigation.
class SavingsTab extends StatelessWidget {
  const SavingsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const SavingsHomePage();
  }
}
